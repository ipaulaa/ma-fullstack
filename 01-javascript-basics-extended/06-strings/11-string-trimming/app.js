const rawText = '   Paula Rodrigues  ';

const trimmedText = rawText.trim();
const leftTrimmedText = rawText.trimStart();
const rightTrimmedText = rawText.trimEnd();

console.log(`|${rawText}|`);
console.log(`|${trimmedText}|`);
console.log(`|${leftTrimmedText}|`);
console.log(`|${rightTrimmedText}|`);
